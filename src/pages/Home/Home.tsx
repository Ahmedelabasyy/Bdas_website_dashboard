import { FC } from "react";

const Home: FC = () => {
  return (
    <div
      className="pageStyle"
      style={{ backgroundColor: "var(--bgColor)", height: "100vh" }}
    >
      Home
    </div>
  );
};

export default Home;
